# WMS 功能修复与优化计划

## 1. 文档目的

本文基于当前工作区代码，整理影响库存准确性、商城交易闭环、采购协同、权限体验和生产交付的修复项。实施顺序以“资金与库存正确性优先、业务闭环其次、体验优化最后”为原则。

## 2. 当前范围

- `wms-server/`：Spring Boot、JPA、库存与商城业务、微信支付。
- `wms-web/`：WMS 与商城管理后台。
- `wms-miniapp/`：仓库作业小程序。
- `wms-shopping-miniapp/`：商城购物小程序。

## 3. P0：上线前必须修复

### 3.1 商城支付与库存一致性

**现状**：`MarketService.createOrder/prepay` 不校验或预占库存，支付回调 `markPaid` 才扣库；取消与支付回调并发时可能出现已付款但订单未落单。

**修复方案**：

1. 新增 `inventory_reservations`，记录订单、物品、仓库、数量、状态和失效时间。
2. 下单时悲观锁定库存并预占；可售量统一为现存量减有效预占量。
3. 支付成功将预占转为正式出库；超时或确认未支付后释放预占。
4. 支付、取消、回调使用订单锁和幂等键；已支付订单取消必须转退款。
5. 后台同时依据 `orderStatus`、`payStatus` 控制审核、发货和取消按钮。

**验收标准**：并发下单不超卖；重复回调不重复扣库；支付与取消并发后资金、订单、库存状态一致。

**实施状态（2026-09-19）：进行中**

- [x] `P0-3.1a`：新增 `inventory_reservations` 实体；下单时按物品锁定 FIFO 库存行并预占，可售库存统一为物理库存减未过期的 HELD 预占。
- [x] `P0-3.1b`：支付和货到付款审核会消费预占；用户取消、审核拒绝、支付超时会释放预占。定时任务会关闭过期未支付订单。
- [x] `P0-3.1c`：取消后迟到的支付回调会保留 PAID 资金事实但不扣库存，并由自动退款任务处理；线上已支付订单的库存也改为退款成功后再回滚。
- [ ] `P0-3.1d`：为 `inventory_reservations` 提供 Flyway 基线/迁移并完成 MySQL 并发压测；完成前不得将本项整体关闭。

**本次验证**：`cd wms-server && mvn -q -Dtest=MarketServiceIntegrationTest test` 通过（90 项），新增用例覆盖预占可售量、取消释放、过期释放与取消后支付自动退款。

### 3.2 修复反审、红冲与流水成本

**现状**：`DocumentService.reverseInventory` 对销售出库反审时按销售价入库；红冲单按普通 IN/OUT 执行，会污染库存成本、利润和收发存报表。商城 OUT 流水数量又与普通出库的负数约定不一致。

**修复方案**：

- 库存流水增加 `sourceDocumentId`、`sourceLineId`、`reversalOfTransactionId`、`operator`。
- 冲销按原流水逐笔写入相反数量和相反金额，不重新计算成本。
- 增加 `reverse_in/reverse_out` 类型，统一“入库正、出库负”。
- 原单记录红冲关系并禁止重复反审、重复红冲。
- 编写历史流水校正脚本，执行前备份并输出差异报告。

**验收标准**：冲销前后库存数量和金额净变化为零；销售利润不包含红冲、调拨或报损流水。

**实施状态（2026-09-19）：进行中**

- [x] `P0-3.2a`：商城 `deductStock` 已改为“入库正、出库负”的统一流水数量约定，并新增支付出库流水符号/汇总数量回归测试。
- [x] `P0-3.2b`：库存流水已记录原单、原行、原流水和操作人；反审/红冲按未冲销的原流水数量和成本生成 `reverse_in/reverse_out`，不计销售/利润。红冲单关联原单并拒绝重复红冲。
- [x] `P0-3.2c`：新增 Flyway `V3__normalize_legacy_reverse_transactions.sql`，仅按既有数量符号将历史 `reverse` 规范为 `reverse_in/reverse_out`，不改数量、金额或利润；新增只读 `test-artifacts/reconcile_inventory_transactions.sql` 输出旧类型、账实差异和未关联冲销行，要求上线前后留存报告。

**本次验证**：`cd wms-server && mvn -q -Dtest=AdjustmentIntegrationTest test` 通过；新增高销售价出库反审成本恢复、红冲金额恢复和重复红冲拒绝测试。

### 3.3 修正报表口径

**现状**：历史收发存仅读取目标月份前一个月流水，期末使用当前库存；库龄算法未正确消费历史出库且分组键不含批次；“今日销售额”实际包含调拨和报损。

**修复方案**：

- 使用月结快照或截止时间流水重建期初、期末数量和金额。
- 库龄按物品、仓库、库位、批次维护剩余入库层。
- 将销售额、业务出库额、调拨额、损溢额分别统计。
- 库存品种按 `itemId` 去重，不按库存行计数。

**验收标准**：历史月份查询不受当前库存变化影响；报表满足“期初 + 入库 - 出库 = 期末”。

**实施状态（2026-09-19）：进行中**

- [x] `P0-3.3a`：收发存改为从 1970 年起至查询月末的库存流水重建，期末不再读取当前 `inventory` 快照；收发存金额统一使用库存成本口径。
- [x] `P0-3.3b`：仪表盘分离 `todaySalesAmount`（仅销售出库）和 `todayOutboundAmount`（全部出库成本），Web 销售卡片已改用前者；库存品种改按 `itemId` 去重。
- [x] `P0-3.3c`：库龄已按物品、仓库、库位、批次逐笔消费带符号流水后维护 FIFO 剩余入库层，再按剩余数量计算加权库龄。

**本次验证**：`cd wms-server && mvn -q -Dtest=ReportControllerIntegrationTest test` 通过，覆盖跨历史月份查询、销售/出库成本口径分离和同批次 FIFO 库龄消费。

### 3.4 收紧生产配置

**现状**：`docker-compose.yml` 未设置生产 Profile，无 Profile 会被 `DemoDataConfig` 视为开发环境并创建默认账号；数据库使用 root 和默认密码，Hibernate 使用 `ddl-auto:update`。

**修复方案**：

- Compose 强制 `SPRING_PROFILES_ACTIVE=prod`，生产配置缺失时启动失败。
- 禁用 H2 Console、演示数据和 mock 登录/支付。
- 使用独立数据库账号和外部密钥，不提供生产默认密码。
- 引入 Flyway/Liquibase，生产使用 `ddl-auto:validate`。

**验收标准**：空配置不能以开发模式启动；生产库不会生成 `admin/admin123` 或演示库存。

**实施状态（2026-09-19）：进行中**

- [x] `P0-3.4a`：新增 `application-prod.yml`，生产环境不再回退到 H2，强制 `ddl-auto=validate` 并关闭 H2 Console。
- [x] `P0-3.4b`：新增 `ProductionSafetyConfig`；即使通过环境变量覆盖，也会拒绝在 `prod` 开启微信登录/支付 mock、H2 Console 或 `ddl-auto` 非 `validate` 的启动方式。
- [x] `P0-3.4c`：Compose 固定 `SPRING_PROFILES_ACTIVE=prod`，改用 `MYSQL_USER` 应用账号，移除默认数据库口令；微信支付 PEM 通过 Compose secrets 挂载，`.env.example` 与部署文档已同步。
- [x] `P0-3.4d`：引入 Flyway；`V1` 为历史基线、`V2` 引入预占/冲销追溯字段、`V3` 规范历史冲销类型。生产库以 V1 基线化后只执行增量，测试环境关闭 Flyway 保持 create-drop 隔离。

**本次验证**：`docker compose config -q`（提供完整测试变量）通过；空变量配置被 Compose 拒绝；空 H2 库执行 V1→V3 后，JPA `validate` 和应用启动通过；`cd wms-server && mvn -q test` 通过。当前环境未提供可用 MySQL/Docker 守护进程，MySQL 实例演练仍须在发布环境完成。

**补充修复（2026-09-20）**：无 Profile 的本地 `application.yml` 已将 `wechat.pay.mock` 默认恢复为 `true`，避免 `mvn spring-boot:run` 在未配置真实微信商户参数时创建支付 SDK 失败；`prod` Profile 仍固定 `false` 并由 `ProductionSafetyConfig` 拒绝环境变量覆盖。

## 4. P1：核心业务闭环

### 4.1 商城客户开户

当前虽定义 `CUSTOMER` 角色，但 `UserRequest` 只允许 ADMIN/WAREHOUSE，购物小程序对未绑定微信也只有提示。应新增微信首次登录注册、CUSTOMER 创建、账号绑定/解绑、手机号验证和注销流程。

**实施状态（2026-09-20）：部分完成**

- [x] `P1-4.1a`：`/auth/wx-login` 对未绑定微信签发仅 5 分钟有效、一次性消费的 `bindTicket`；`/auth/wx-bind` 不再接受客户端指定 openid，防止伪造绑定。
- [x] `P1-4.1b`：新增 `/auth/wx-register`，服务端固定创建 `CUSTOMER`；商城小程序已提供用户名、昵称、密码的首次开户页，仓库小程序已改为传递 `bindTicket`。
- [ ] `P1-4.1c`：手机号验证码、解绑、账号注销/个人信息导出尚未实现；这需要短信服务、冷静期及数据保留策略，不能以模拟号码替代。

**本次验证**：`AuthControllerWxTest` 覆盖一次性凭据、既有账户绑定与 CUSTOMER 开户；两个小程序 `build:mp-weixin` / `build:wechat` 通过。

### 4.2 采购闭环

当前采购申请批准后停留在 `APPROVED`，Web 端也缺少创建、编辑和取消入口。需要补齐：

`库存预警 → 采购申请 → 审批 → 采购订单 → 分批收货 → 入库单`

各阶段应保存来源单号、已收数量、未收数量、供应商价格和交期。

**实施状态（2026-09-20）：部分完成**

- [x] `P1-4.2a`：管理后台已提供采购申请创建、供应商/仓库/物品明细录入和草稿/驳回单取消入口；沿用现有 `purchase-request:write/review` 后端权限。
- [ ] `P1-4.2b`：采购订单、分批收货、到货质检与自动生成入库单尚未建模，采购申请批准仍停在 `APPROVED`；不可将现有申请页面误称为采购闭环。

### 4.3 权限与职责分离

- 拆分 `inventory:scan`，解决 WAREHOUSE 菜单可见但接口要求 ADMIN 的冲突。
- 增加采购、审核、财务、审计、客服等角色。
- 增加用户仓库数据范围，并禁止制单人审核自己的单据。
- 权限判断必须由后端强制，前端菜单只负责展示。

**实施状态（2026-09-19）：进行中**

- [x] `P1-4.3a`：新增 `inventory:scan` 并仅授予 ADMIN；扫码接口、WMS Web 菜单/路由和仓库小程序入口统一使用该职责边界，WAREHOUSE 不再看到会 403 的直接扫码表单。
- [x] `P1-4.3b`：新增 PROCUREMENT、AUDITOR、FINANCE、CUSTOMER_SERVICE、CUSTOMER 角色，并将固定权限矩阵暴露给用户管理页；服务端 `UserRequest` 同步校验，前端可创建和查看各角色。
- [ ] `P1-4.3c`：用户仓库数据范围和制单/审核职责分离尚未实现。当前历史单据没有统一制单人字段，必须先做数据迁移和所有查询的范围过滤，不能只在菜单上隐藏。

**本次验证**：`RolePermissionsTest` 通过（ADMIN 有且 WAREHOUSE 无 `inventory:scan`）；`cd wms-web && npm run build` 通过。

### 4.4 售后与退款

退款接口已有后端实现，但管理后台没有操作入口；目前退款成功会直接回滚库存，未验证实物退回。应新增售后申请、退货物流、收货质检、可售/残次品处理，再触发库存恢复和退款完成。

**实施状态（2026-09-20）：部分完成**

- [x] `P1-4.4a`：商城订单列表/详情已显示支付、退款中、已退款状态和退款单号，并提供已支付订单的退款原因输入与发起入口。
- [ ] `P1-4.4b`：售后申请、退货物流、仓库签收、质检和良品/残次库存去向未实现；当前退款前端会明确提示已发货订单需先完成实物退回确认，后端库存回滚策略仍需在该闭环完成后收紧。

## 5. P2：体验与工程优化

- [ ] 为仓库、库位、分类补充管理页面及批量导入。
- [ ] 单据、库存、日志、采购申请统一服务端分页、筛选和排序。
- [x] `P2-5.1`：物品页改为服务端分页（默认 20 条，搜索重置到第一页），不再固定请求第 1 页 100 条。
- [ ] 操作日志增加业务对象、IP、变更前后值和脱敏请求摘要，查询改为数据库分页。
- [x] `P2-5.2`：OCR 按钮和成功提示明确标为“演示识别/需人工核验”，服务端成功消息同步说明为模拟结果。
- [x] `P2-5.3`：移除后台 `min-width:1180px`，侧栏支持折叠/响应式收起，窄屏压缩内容区并为表格保留横向滚动。
- [x] `P2-5.4`：商城订单页展示支付、退款中、已退款状态，增加退款发起入口；退款失败状态需支付渠道回调提供明确失败码后再建模。
- [x] `P2-5.5`：后端 CORS 改为仅允许 `localhost`/`127.0.0.1` 的本机开发端口模式，并在 Spring Security 启用同一 CORS 配置；修复 Vite 自动从 5173 递增到 5174 时登录接口返回 403。
- [ ] 优化 Vite 分包，降低 Ant Design 和 ECharts 首屏体积（当前 `npm run build` 仍有大包警告）。

## 6. 测试要求

必须新增以下自动化用例：

1. 同一库存并发预占、支付、释放与扣减。
2. 支付回调与取消并发、重复回调、库存不足和退款回调。
3. 入库、出库、退货、调拨、损溢、反审和红冲的数量/金额不变量。
4. 多批次 FIFO/库龄以及跨历史月份收发存。
5. ADMIN、WAREHOUSE、CUSTOMER 及新增角色的接口权限矩阵。
6. 采购申请到分批收货的完整链路。
7. 管理后台和两个小程序的登录、制单、盘点、下单、支付、发货 E2E。

每次提交至少执行：

```bash
cd wms-server && mvn test
cd ../wms-web && npm run build
cd ../wms-miniapp && npm run build:mp-weixin
cd ../wms-shopping-miniapp && npm run build:wechat
```

## 7. 推荐实施顺序

| 阶段 | 内容 | 完成标志 |
| --- | --- | --- |
| 第一阶段 | 流水符号、反审红冲、报表、生产配置 | 库存核账通过，生产配置安全 |
| 第二阶段 | 库存预占、支付取消状态机、客户开户 | 商城可供真实客户完成交易 |
| 第三阶段 | 采购闭环、售后退款、角色数据权限 | 采购与售后业务可追溯 |
| 第四阶段 | 分页、审计、响应式、性能优化 | 大数据量和多端体验达标 |

## 8. 完成定义

修复项只有同时满足以下条件才可关闭：代码与数据库迁移已合并；权限及异常路径已覆盖；自动化测试通过；接口和页面行为一致；历史数据已验证或迁移；README、DEVELOPMENT 和部署配置同步更新；涉及库存金额的改动已经完成上线前后对账。
