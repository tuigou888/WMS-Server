# WMS 网站功能测试报告

- **项目**：仓库进销存管理系统（WMS）
- **测试日期**：2026-07-19
- **测试人员**：Codex 自动化测试
- **测试环境**：Linux、Java 21.0.11、Spring Boot 3.3.12、H2 内存数据库、Node/Vite 6.4.3、Google Chrome Headless
- **前端地址**：`http://127.0.0.1:5173`
- **后端隔离测试地址**：`http://127.0.0.1:18088/api/v1`
- **代码版本说明**：在当前工作区未提交代码上进行测试；业务测试使用独立端口与独立 H2 内存库，未修改正式数据库。

## 1. 测试结论

**结论：核心后端业务基本可用，但网站当前不能判定为“全部正常”，不建议按当前版本直接发布。**

主要业务接口（登录、物品、往来单位、仓库、扫码出入库、单据、调拨、盘点、报表、二维码、Excel、用户管理）绝大多数工作正常；但发现：

1. **高严重度前端缺陷**：管理员进入“用户与权限”后切换到其他菜单，React 抛出 `TypeError: destroy is not a function`，页面无法正常继续使用。
2. **高严重度部署缺陷**：Docker 暴露/映射 `8080`，应用默认监听 `8088`；且 Web 的 Nginx 镜像没有 `/api/v1` 反向代理配置，按现有 `docker compose up --build` 很可能无法正常登录和调用 API。
3. **中严重度成本精度缺陷**：调拨到空库存后，目标库存平均成本未严格继承源库存平均成本，产生四位小数漂移。
4. **低严重度接口语义问题**：退出后访问 `/auth/me` 返回 HTTP 400，而不是统一的 HTTP 401。

## 2. 测试范围与方法

### 2.1 执行内容

- Maven 单元测试
- Vite 生产构建
- Spring Boot 服务启动与健康检查
- REST API 自动化功能测试与负向校验
- Chrome 无头浏览器登录、菜单和页面渲染测试
- 权限、状态机、库存不足和参数边界测试
- Docker 与前后端代理配置静态检查

### 2.2 未覆盖范围

- MySQL 真实持久化、数据迁移和大数据量性能
- 多用户并发、库存并发扣减、压力测试
- 微信小程序端
- 移动端/窄屏适配（当前 CSS 设置 `body min-width: 1180px`）
- Docker 镜像实际拉取和完整容器联调（本次未执行外部镜像下载）
- 长时间稳定性、网络中断恢复、安全渗透测试

## 3. 测试结果汇总

| 测试类别 | 结果 | 说明 |
|---|---:|---|
| 后端单元测试 | 1/1 通过 | `InventoryCostCalculatorTest` 通过 |
| 前端生产构建 | 通过 | 构建成功；主 JS 约 1.88 MB，存在分包体积警告 |
| API 自动化测试 | 42/44 通过 | 1 个成本精度失败，1 个退出状态码语义失败 |
| 浏览器基础测试 | 登录、菜单显示和页面初次渲染通过 | 管理员登录成功，12 个菜单项显示；切换离开“用户与权限”时崩溃 |
| 部署配置检查 | 不通过 | 服务端口和 Nginx API 代理配置不一致/缺失 |

## 4. 功能测试明细

### 4.1 基础可用性与认证

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 健康检查 | 通过 | `/health` 返回 `UP` |
| 未登录访问物品接口 | 通过 | 返回 HTTP 401 |
| 管理员登录 | 通过 | 返回 ADMIN、Token、43200 秒有效期 |
| 仓库操作员登录 | 通过 | 返回 WAREHOUSE、Token |
| 错误密码登录 | 通过 | 返回 HTTP 400，未签发 Token |
| 查询当前用户 | 通过 | 正确返回用户名、姓名、角色 |
| 退出使 Token 失效 | 基本通过 | Token 已失效；但 `/auth/me` 返回 400 而非 401 |

### 4.2 权限控制

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 操作员查询用户列表 | 通过 | 请求被拒绝 |
| 操作员新增仓库 | 通过 | 请求被拒绝 |
| 操作员审核入库单 | 通过 | 请求被拒绝 |
| 管理员审核单据/调拨/盘点 | 通过 | 审核状态正确更新 |
| 操作员菜单权限 | 代码/API验证通过 | “用户与权限”仅对 ADMIN 构造；因前端崩溃后的级联影响，未完成同一浏览器会话的最终复测 |

> 注：权限不足目前多以 HTTP 400 返回。功能上能拦截，但更规范的语义应为 HTTP 403。

### 4.3 基础资料

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 演示数据初始化 | 通过 | 3 个物品、1 个仓库、3 行库存成功生成 |
| 新增物品 | 通过 | 保存成功 |
| 重复物品编码 | 通过 | 被拒绝 |
| 更新物品 | 通过 | 名称、安全库存等成功更新 |
| 删除无库存物品 | 通过 | 删除后无法查询 |
| 新增供应商/客户 | 通过 | 保存成功 |
| 往来单位类型筛选 | 通过 | SUPPLIER 筛选正常 |
| 更新往来单位 | 通过 | 联系人更新成功 |
| 管理员新增/更新仓库 | 通过 | 保存成功 |

### 4.4 库存与扫码出入库

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 扫码入库 | 通过 | 库存从 0 增至 10，平均成本 12.5 |
| 扫码出库 | 通过 | 库存从 10 降至 7，利润 22.5 |
| 超库存出库 | 通过 | 被拒绝，库存未被扣成负数 |
| 零数量入库 | 通过 | 参数校验拒绝 |
| 库存列表与分页 | 通过 | `pageSize=2` 返回 2 条 |
| 库存流水 | 通过 | 入、出、调拨、盘点流水均可查询 |

### 4.5 入库单/出库单

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 操作员创建入库草稿 | 通过 | 状态 DRAFT |
| 管理员审核入库单 | 通过 | 状态 APPROVED |
| 执行入库单 | 通过 | 状态 COMPLETED，库存和移动均价更新 |
| 重复执行已完成单据 | 通过 | 被状态机拒绝 |
| 出库单完整流程 | 通过 | DRAFT → APPROVED → COMPLETED |
| 取消草稿单 | 通过 | 状态 CANCELLED |
| 入库单选择客户 | 通过 | 类型规则校验拒绝 |

### 4.6 调拨与盘点

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 调拨状态流 | 通过 | DRAFT → APPROVED → COMPLETED |
| 调拨数量变化 | 通过 | 源仓 10→6，目标仓 0→4 |
| 调拨流水关联 | 通过 | 生成相同单号的 `transfer_out`、`transfer_in` |
| 调拨成本继承 | **失败** | 源成本 13.5417，目标成本 13.5425 |
| 同仓调拨 | 通过 | 被业务规则拒绝 |
| 盘点完整流程 | 通过 | 快照、录入、审核、执行均正常 |
| 盘点差异调整 | 通过 | 目标库存从 4 调整为 5，生成 `adjust_in` |

### 4.7 报表、二维码与 Excel

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 仪表盘报表 | 通过 | 库存品种、数量、金额、预警等字段正常 |
| 库存预警 | 通过 | 返回低于安全库存的物品 |
| 利润报表 | 通过 | 出库后利润流水可查询 |
| 二维码 JSON/Data URL | 通过 | 内容与物品编码一致 |
| 二维码 PNG | 通过 | 返回合法 PNG 文件 |
| Excel 导出 | 通过 | 生成有效 `.xlsx` 文件 |
| Excel 回导 | 通过 | 4 条已有物品成功更新，0 条跳过 |

### 4.8 用户管理

| 用例 | 结果 | 实际结果 |
|---|---|---|
| 管理员创建用户 | 通过 | 新用户创建成功 |
| 新用户登录 | 通过 | 角色为 WAREHOUSE |
| 更新并停用用户 | 通过 | 停用后无法登录 |
| 用户管理页面切换 | **失败** | 离开页面时 React 运行时异常，应用无法继续正常导航 |

## 5. 缺陷清单

### BUG-01：离开“用户与权限”页面时前端崩溃

- **严重度**：高
- **优先级**：P0/P1
- **位置**：`wms-web/src/pages/UsersPage.jsx:5`
- **复现步骤**：
  1. 使用 `admin/admin123` 登录。
  2. 点击“用户与权限”。
  3. 再点击“仪表盘”或其他菜单。
- **实际结果**：Chrome 控制台抛出 `TypeError: destroy is not a function`；页面只剩残留内容，菜单切换和退出均不能正常继续。
- **原因**：`useEffect(load, [])` 中的 `load` 直接返回 Promise，React 将其当作 effect cleanup；组件卸载时尝试把 Promise 当函数调用。
- **修复建议**：改为 `useEffect(() => { load() }, [])`，并增加组件卸载后的异步更新保护。

### BUG-02：调拨到空库存时平均成本发生精度漂移

- **严重度**：中
- **优先级**：P1
- **位置**：`wms-server/src/main/java/com/wms/service/InventoryService.java:36-40`
- **复现数据**：源平均成本 `13.5417`，调拨数量 `4`。
- **预期结果**：目标库存平均成本严格继承 `13.5417`。
- **实际结果**：目标总金额先四舍五入为 `54.17`，再用 `54.17 / 4` 计算，得到 `13.5425`。
- **影响**：多次跨仓调拨后可能累计成本与利润误差。
- **修复建议**：目标为空库存时直接写入源 `unit` 作为平均成本；或内部金额保持更高精度，仅在展示/记账边界舍入。

### BUG-03：Docker 服务端口不一致

- **严重度**：高
- **优先级**：P0/P1
- **位置**：
  - `wms-server/src/main/resources/application.yml`：默认 `8088`
  - `wms-server/Dockerfile`：`EXPOSE 8080`
  - `docker-compose.yml`：`8080:8080`
- **实际影响**：容器中的 Spring Boot 默认监听 8088，但 Compose 将主机 8080 转发到容器 8080，API 无法按文档地址访问。
- **修复建议**：Compose 为后端设置 `SERVER_PORT: 8080`，或统一将 Dockerfile/Compose 改为 8088。

### BUG-04：生产 Web 容器缺少 API 反向代理

- **严重度**：高
- **优先级**：P0/P1
- **位置**：`wms-web/Dockerfile`
- **问题**：前端 Axios 使用相对地址 `/api/v1`，开发环境依赖 Vite proxy；生产 Nginx 镜像只复制静态文件，没有将 `/api/v1` 转发到 `wms-server`。
- **实际影响**：通过 Compose 访问 Web `3000` 后，登录请求会打到 Web Nginx 自身并得到 404。
- **修复建议**：增加 Nginx 配置，将 `/api/v1/` 代理到 `http://wms-server:8080/api/v1/`，并在 Dockerfile 中复制配置。

### BUG-05：退出后 `/auth/me` 状态码不统一

- **严重度**：低
- **优先级**：P3
- **位置**：`SecurityConfig.java:43`、`AuthController.java:8`
- **预期结果**：失效 Token 返回 HTTP 401。
- **实际结果**：由于 `/auth/**` 全部放行，`/auth/me` 进入控制器后抛业务异常并返回 HTTP 400。
- **修复建议**：只放行 `/auth/login`（以及确需匿名的接口），让 `/auth/me` 受认证过滤器保护。

### ISSUE-06：接口与端口文档不一致

- **严重度**：低
- **位置**：`DEVELOPMENT.md`、`README.md`
- **问题**：文档描述 API 默认端口为 8080、扫码接口为 `/stock-in/scan` 和 `/stock-out/scan`；当前实现为端口 8088、接口 `/stock/in/scan` 和 `/stock/out/scan`。
- **建议**：以实际实现为准统一 README、开发文档、Docker 和前端代理配置。

## 6. 非阻断性风险与改进建议

1. **自动化测试覆盖不足**：后端目前仅有 1 个单元测试。建议增加 Spring Boot 集成测试，覆盖单据状态机、权限、调拨成本、盘点和库存并发。
2. **前端无测试脚本**：建议引入 Vitest + React Testing Library，以及 Playwright/Cypress 端到端测试。
3. **前端包体较大**：构建主 JS 约 `1,875.63 kB`（gzip `582.02 kB`）。建议路由/页面懒加载、拆分 Ant Design 与 Recharts。
4. **金额精度策略需统一**：明确库存数量、单价、金额、平均成本各自的数据库精度和舍入时点。
5. **HTTP 状态码规范化**：认证失败用 401，权限不足用 403，业务参数/状态错误用 400 或 409。
6. **删除约束提示**：建议为已被单据或库存引用的物品/往来单位提供明确业务错误，而非依赖数据库异常。

## 7. 发布建议

- **当前建议：暂缓发布。**
- 发布前至少完成 BUG-01、BUG-03、BUG-04；BUG-02 应在正式产生财务/利润数据前修复。
- 修复后建议执行一次完整回归：
  - 管理员逐个切换全部菜单并退出；
  - 使用 Docker Compose 从全新环境启动；
  - 从 Web `3000` 完成登录、扫码入库、单据审核执行；
  - 验证跨仓多次调拨后的总库存金额守恒。

## 8. 测试产物

- `test-artifacts/api-results.json`：API 自动化原始结果
- `test-artifacts/ui-results.json`：Chrome UI 自动化原始结果
- `test-artifacts/login-page.png`：登录页截图


---

## 9. 修复回归状态（2026-07-19）

本报告发现的 BUG-01 至 BUG-05 及 ISSUE-06 均已修复。修复内容与回归结果见 `FIX_REPORT.md`。 当前项目规定的统一 API 端口为 `8088`。当前发布阻断项已解除；正式发布前仍建议在可拉取镜像的环境执行一次完整 `docker compose up --build` 联调。
