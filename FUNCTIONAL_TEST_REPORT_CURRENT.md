# WMS 当前系统功能测试报告

- 测试日期：2026-07-23
- 测试范围：系统业务功能、接口、浏览器页面、角色权限
- 测试环境：Java 21、Spring Boot 3.3.12、H2 内存数据库、React 18、Vite 6.4.3、Google Chrome Headless 150
- 后端测试地址：`http://127.0.0.1:8088/api/v1`
- 前端测试地址：`http://127.0.0.1:5173`
- 范围说明：本报告不包含 Docker、MySQL 部署、生产环境配置或容器联调测试。

## 1. 测试结论

本轮覆盖的系统功能均测试通过，未发现遗留的阻断性功能错误：

| 测试类别 | 通过 | 失败 | 结果 |
|---|---:|---:|---|
| 后端单元测试 | 2 | 0 | 通过 |
| API 业务回归 | 70 | 0 | 通过 |
| 浏览器 UI 回归 | 29 | 0 | 通过 |
| **合计功能检查项** | **101** | **0** | **全部通过** |

## 2. 功能覆盖

### 2.1 登录、退出与权限

- 管理员、仓库操作员登录
- 错误密码拒绝登录
- 当前用户查询与退出后 Token 失效
- 未登录访问返回 401
- 操作员访问管理员接口返回 403
- 管理员显示 12 个菜单；操作员隐藏“用户与权限”菜单
- 管理员退出后正常返回登录页

### 2.2 基础资料

- 物品新增、查询、搜索、分页、修改、删除
- 物品编码重复和必填参数校验
- 物品分类查询
- 供应商、客户、双类型往来单位新增、筛选、修改、删除
- 往来单位编码重复校验
- 仓库新增、更新、启用列表和编码重复校验
- 全部库位及按仓库查询

### 2.3 库存与扫码业务

- 扫码入库、扫码出库
- 移动平均成本与销售利润计算
- 库存不足时阻止出库
- 按物品、仓库、库位和批次查询库存
- 库存列表、分页和完整库存流水
- 带批次库存的创建、扣减和查询

### 2.4 入库单与出库单

- 操作员创建草稿
- 管理员审核通过或驳回
- 操作员执行已审核单据
- 草稿取消
- 已完成单据禁止重复执行
- 单据列表和详情查询
- 入库供应商类型校验
- 入库后移动平均成本更新

### 2.5 库存调拨

- 跨仓调拨创建、审核、执行和列表查询
- 调拨成本继承
- 带批次库存调拨
- 驳回调拨单
- 禁止同仓调拨
- 浏览器新建调拨窗口显示“批次号（可选）”输入项

### 2.6 库存盘点

- 按仓库创建盘点单
- 自动生成账面库存明细
- 按物品、库位和批次录入实盘数量
- 审核、执行差异调整和驳回
- 执行后库存数量及库存流水正确更新

### 2.7 报表与工具

- 仪表盘库存品种、库存金额、销售额和预警数据
- 库存预警、利润和异常报表
- 二维码数据、Data URL 和 PNG 下载
- Excel 物品导出、更新导入和新增导入
- OCR 文件识别返回与空文件校验

### 2.8 用户管理

- 管理员创建用户
- 新用户登录
- 用户资料、角色、密码和启用状态更新
- 停用用户后禁止登录
- 用户列表查询

### 2.9 浏览器页面

Chrome 无头浏览器实际验证以下页面可加载和切换：

- 仪表盘
- 物品档案
- 供应商 / 客户
- 扫码入库、扫码出库
- 入库 / 出库单
- 库存调拨
- 库存盘点
- 库存管理
- 报表中心
- 二维码与 Excel
- 用户与权限

页面切换过程中未捕获 JavaScript 异常、浏览器错误日志或 HTTP 5xx 响应。

## 3. 本轮发现并修复的问题

### 带批次库存无法正确调拨和盘点

原实现的调拨和盘点固定按无批次库存查询。当库存由扫码或单据以批次号入库后，会出现以下问题：

- 调拨找不到实际存在的带批次源库存；
- 盘点无法正确区分同一物品、同一库位下的多个批次；
- 调拨目标库存不能保持原批次。

修复后，调拨和盘点的请求、单据明细、库存查询、执行及差异调整全程传递 `batchNo`，并按“物品 + 库位 + 批次”区分库存。回归结果：

- 带批次调拨完成，目标库存 `4.0`，目标平均成本 `13.5417`；
- 带批次盘点完成，差异调整后库存 `5.0`；
- API 70 项和 UI 29 项全部通过。

### 浏览器缺少站点图标

浏览器曾请求不存在的 favicon 并产生 404 错误日志。现已增加站点图标，重新回归后浏览器错误日志为 0。

## 4. 测试产物与复现

- API 脚本：`test-artifacts/run_api_regression.py`
- API 结果：`test-artifacts/api-results-current.json`
- UI 脚本：`test-artifacts/run_ui_regression.py`
- UI 结果：`test-artifacts/ui-results-current.json`
- UI 截图：`test-artifacts/ui-admin-dashboard-current.png`

后端启动后可执行：

```bash
python3 test-artifacts/run_api_regression.py http://127.0.0.1:8088/api/v1
```

前后端均启动后可执行：

```bash
python3 test-artifacts/run_ui_regression.py http://127.0.0.1:5173/
```

## 5. 功能测试边界

- OCR 当前返回模拟识别结果，本轮验证的是上传、返回结构和错误校验，不代表真实 OCR 准确率。
- 自动化使用 H2 内存数据库，不包含 MySQL 数据兼容性和持久化验证。
- 未执行多用户高并发扣减、压力、长时间稳定性和安全渗透测试。
- 浏览器测试覆盖所有菜单页面、角色权限和关键批次输入控件；完整增删改业务主要由 API 端到端回归覆盖。

## 6. 最终判定

在上述功能范围和测试环境内，当前 WMS 系统的登录鉴权、基础资料、扫码出入库、库存、单据、调拨、盘点、报表、二维码、Excel、OCR 模拟流程和用户权限功能均可正常使用。本结论仅代表当前功能回归通过，不等同于生产部署或生产就绪认证。
