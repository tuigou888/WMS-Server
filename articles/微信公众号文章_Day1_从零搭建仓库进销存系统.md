# 【Day 1】从零搭建一套仓库进销存管理系统：我是如何用 Spring Boot + React 搞定库存成本核算的

> 仓库管理听起来简单，但「移动加权平均成本」这一关就卡住了多少人？

> 📌 本文是「从零搭建仓库进销存系统」系列的 **Day 1**。这一篇先啃下最硬的骨头——**成本核算**。下一篇 Day 2，我们聊聊记完账之后更难的事：**调拨、盘点、智能预警和权限**。

---

## 一、一个真实的需求

朋友开了一家小型设备贸易公司，仓库里堆着几百种型号的设备和配件。

他问我："能不能帮我做个系统，扫码入库、扫码出库，随时能看到库存数量和**利润**？"

这个需求乍看很常规：物品管理、入库出库、库存查询。但深入聊下去，真正的难点浮现了——

**利润怎么算？**

他说："我进货价 10 块，卖了 25 块，那利润就是 15 块啊。"

我说："如果同一个东西，你第一批进了 100 个，单价 10 块；第二批进了 200 个，单价 12 块。现在出库 50 个，每个成本是多少？"

他沉默了。

这就是仓库进销存系统最核心、也最容易出错的地方——**成本核算**。

---

## 二、技术选型：为什么选这些？

需求明确之后，技术选型就顺理成章了：

| 层级 | 选型 | 理由 |
|------|------|------|
| 后端框架 | Spring Boot 3 + JPA | 稳定、生态成熟、开发效率高 |
| 前端 | React + Ant Design + Vite | 组件丰富、适合管理后台场景 |
| 数据库 | MySQL 8.0 / H2（开发） | 事务支持，ACID 保证 |
| 构建 | Maven | 传统 Java 项目的标配 |
| 部署 | Docker Compose | 一键启动所有服务 |

> 这里没有用微服务，没有用 Redis，没有用消息队列。因为对于一个年吞吐量几万单的小型仓库来说，**架构的复杂度应该匹配业务的复杂度**。过度设计是初学者最容易犯的错误。

---

## 三、核心难点：移动加权平均成本

### 3.1 什么是移动加权平均？

这是仓库成本核算中最常用的方法之一。每次入库时，重新计算一次平均成本：

```
新平均成本 = (库存原金额 + 本次入库金额) ÷ (库存原数量 + 本次入库数量)
```

**出库时，成本单价从系统取，不允许手动改。**

什么意思？看一个例子就懂了：

| 日期 | 操作 | 数量 | 单价 | 金额 | 库存数量 | 库存金额 | 平均成本 |
|------|------|------|------|------|---------|---------|---------|
| 7/1 | 期初 | 100 | 10 | 1000 | 100 | 1000 | 10.00 |
| 7/5 | 入库 | 200 | 12 | 2400 | 300 | 3400 | **11.33** |
| 7/10 | 出库 50 | — | 11.33 | 566.67 | 250 | 2833.33 | 11.33 |
| 7/15 | 入库 | 150 | 15 | 2250 | 400 | 5083.33 | **12.71** |

关键规则只有两条：

1. **入库** → 数量和金额都增加，平均成本重新计算
2. **出库** → 只扣减数量和金额，**平均成本不变**

### 3.2 代码实现有多简单？

说实话，核心计算逻辑只需要几行代码：

```java
public static BigDecimal averageCost(
        BigDecimal oldQuantity, BigDecimal oldAmount,
        BigDecimal incomingQuantity, BigDecimal incomingAmount) {
    BigDecimal totalQuantity = oldQuantity.add(incomingQuantity);
    if (totalQuantity.signum() == 0) return BigDecimal.ZERO;
    return oldAmount.add(incomingAmount)
        .divide(totalQuantity, 4, RoundingMode.HALF_UP);
}
```

但**真正的复杂度不在计算，而在事务一致性**。

当操作员扫码入库的那一刻，系统要完成：

1. 查找或创建库存记录（带悲观锁）
2. 计算新平均成本
3. 更新库存数量和金额
4. 写入不可篡改的**库存流水**

这四步必须在同一个数据库事务中完成，任何一步失败都要整体回滚。Spring Boot 的 `@Transactional` 注解在这里发挥了关键作用：

```java
@Service
public class InventoryService {
    @Transactional
    public Map<String, Object> stockIn(StockInRequest request) {
        // 1. 查找物品、仓库、库位（不存在则创建）
        // 2. 加悲观锁读取库存行
        // 3. 计算新平均成本
        // 4. 更新库存
        // 5. 写入流水
        // 这五步在一个事务里，要么全成功，要么全回滚
    }
}
```

---

## 四、库存流水：比库存表更重要的一张表

很多初学者做仓库系统，只盯着「库存表」——当前有多少存货。

但真正做业务的人会告诉你：**库存表是结果，流水表才是真相。**

```
CREATE TABLE inventory_transactions (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    item_id         INT NOT NULL,
    warehouse_id    INT NOT NULL,
    transaction_type ENUM('in','out','transfer','adjust') NOT NULL,
    reference_no    VARCHAR(50) NOT NULL,   -- 关联单据号
    quantity        DECIMAL(18,4) NOT NULL,  -- 变动数量（正入负出）
    unit_cost       DECIMAL(18,4) NOT NULL,  -- 变动单价
    total_cost_amount DECIMAL(18,2) NOT NULL, -- 变动金额
    sale_price      DECIMAL(18,4),            -- 售出价
    profit          DECIMAL(18,2),            -- 利润
    balance_quantity DECIMAL(18,4) NOT NULL,  -- 结存数量
    balance_amount  DECIMAL(18,2) NOT NULL,   -- 结存金额
    avg_cost_after  DECIMAL(18,4) NOT NULL,   -- 变动后平均成本
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**为什么流水这么重要？**

- **可追溯**：每一笔库存变动都能追溯到单据、操作人、时间
- **可对账**：期末库存 = 期初 + Σ入库 - Σ出库 ± 调整，缺一分钱都能查
- **可审计**：财务审计时，只看流水表就能还原所有业务

每条流水记录同时保存了**变动后**的结存数量和金额，相当于时刻拍了一张快照。这样即使未来代码改了成本算法，历史数据依然可查。

---

## 五、利润计算：出库时才知道赚了多少

这是朋友最关心的功能。每次出库时，系统自动计算：

```
成本金额 = 出库数量 × 当前平均成本（系统自动）  
销售金额 = 出库数量 × 售出价（人工录入）  
利润 = 销售金额 - 成本金额  
利润率 = 利润 ÷ 销售金额 × 100%
```

看一个真实的扫码出库页面原型：

```
┌──────────────────────────┐
│  ← 返回    扫码出库      │
├──────────────────────────┤
│ 物品: 某型号设备          │
│ 当前库存: 300 个          │
│ 平均成本: ¥14.80 (系统)   │
│                          │
│ 出库数量: [ 10 ] 个       │
│ 售出单价: [ 25.00 ] 元    │
│                          │
│ 成本金额: ¥148.00 (自动)  │
│ 销售金额: ¥250.00 (自动)  │
│ ┌────────────────────┐   │
│ │ 利润: ¥102.00      │   │
│ │ 利润率: 40.8%      │   │
│ └────────────────────┘   │
│  ┌────────────────────┐  │
│  │     确认出库       │  │
│  └────────────────────┘  │
└──────────────────────────┘
```

操作员只需要输入数量和售价，**成本、利润全部自动计算**。这也是仓库数字化最大的价值——让一线操作简单，让管理决策有数据支撑。

---

## 六、架构设计：前后端分离

### 后端分层

```
controller/    # 接收HTTP请求，参数校验
service/       # 业务逻辑，事务管理
repository/    # 数据访问层（JPA）
model/entity/  # 数据实体
dto/           # 请求/响应数据传输对象
```

### 前端页面结构

```
├── 仪表盘 —— 库存总览、预警提醒
├── 物品档案 —— 物品管理、分类、二维码
├── 入库管理 —— 入库单、扫码入库
├── 出库管理 —— 出库单、扫码出库
├── 库存管理 —— 库存查询、流水、调拨
├── 盘点管理 —— 盘点计划、扫码盘点
├── 报表中心 —— 收发存汇总、利润报表
└── 系统管理 —— 用户、权限、日志
```

### 扫码流程（微信小程序端）

```
扫描二维码 → 解析物品编码 → 查询物品详情 → 
输入数量/单价 → 系统自动计算金额 → 确认 → 
更新库存 → 写入流水 → 返回结果
```

---

## 七、踩过的坑与经验教训

### 坑1：并发入库存导致的数据竞争

第一次测试时，两个操作员同时给同一个库位入库，结果库存金额算错了。

**原因**：两个事务同时读到旧的库存记录，各自计算新平均成本，后写入的覆盖了先写入的。

**解决**：用 `SELECT ... FOR UPDATE` 悲观锁，保证同一时刻只有一个事务能修改这条库存记录。

```java
@Query("SELECT i FROM Inventory i WHERE i.item.id = :itemId ...")
@Lock(LockModeType.PESSIMISTIC_WRITE)
Optional<Inventory> findForUpdate(...);
```

### 坑2：单据号重复

系统重启后，单据号又从 0001 开始，和之前的单号重复了。

**原因**：用了进程内的 `AtomicLong` 生成序列号，重启就归零。

**教训**：生产环境必须用数据库序列或 Redis 生成持久化的单号。

### 坑3：库存精度问题

调拨到空库位时，平均成本算出来变成 0。

**原因**：当库存数量为 0 时，除数为 0，导致计算异常。

**修复**：在计算平均成本时，先判断旧库存数量是否为 0：

```java
public static BigDecimal transferAverageCost(
        BigDecimal oldQuantity, BigDecimal oldAmount,
        BigDecimal incomingQuantity, BigDecimal incomingUnitCost) {
    if (oldQuantity.signum() == 0) {
        // 旧库存为空，直接使用入库单价
        return incomingUnitCost.setScale(4, RoundingMode.HALF_UP);
    }
    // 正常计算移动加权平均
    return averageCost(oldQuantity, oldAmount, 
        incomingQuantity, amount(incomingQuantity, incomingUnitCost));
}
```

---

## 八、总结：做项目最重要的是什么？

这个项目从零开始，技术栈不算复杂——Spring Boot + React 是当前最主流的组合之一。但真正让我收获最大的，不是技术本身，而是**理解业务**。

仓库进销存的核心，不是 CRUD，不是页面好不好看，而是：

1. **成本核算方法**——移动加权平均、先进先出、个别计价，选哪个？
2. **数据一致性**——并发操作时库存账会不会乱？
3. **可追溯性**——半年后能不能查清楚一笔出入库的来龙去脉？
4. **简单性**——操作员能不能在 10 秒内完成一次扫码入库？

**技术服务于业务，而不是反过来。**

如果你也在做类似的系统，我的建议是：先把这个 Excel 算清楚——

| 日期 | 入库 | 出库 | 库存数量 | 库存金额 | 平均成本 |
|------|------|------|---------|---------|---------|
| — | — | — | — | — | — |

如果 Excel 都算不对，代码写得再漂亮也没用。

---

> **项目地址**：GitHub（私信获取）
> **技术栈**：Spring Boot 3 + JPA + React + Ant Design + MySQL
> **功能**：物品管理、扫码入库/出库、调拨、盘点、成本核算、利润报表

---

## 📮 预告：Day 2 讲什么？

成本核算搞定了，账能算对了。但真实的仓库运营还有一堆「记完账之后」的难题：

- **调拨**：同一批货从 A 仓搬到 B 仓，成本价怎么带过去？
- **盘点**：账面 300 个，实盘 297 个，那 3 个差异怎么入账？
- **智能预警**：库存低于安全线时，系统能不能主动告诉我该补多少货？
- **权限**：老板能看利润，操作员只能扫码，这个边界怎么划？

这些才是把「能用的 Demo」变成「能交付的系统」的关键。**Day 2 见。**

---

*如果这篇文章对你有帮助，欢迎点赞、转发、在看。你的支持是我持续分享的动力！*