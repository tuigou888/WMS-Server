# WMS 进销存系统 — 功能差距审计报告

> 审计基线：`master` 分支初始提交（commit `3aa96f3`）。对标参考：jshERP（华夏ERP）、RuoYi-WMS、jeewms、管家婆、旺店通等成熟开源/商业进销存 + WMS 系统。
> 本文不重复 `README.md` 已实现的功能，仅列出**缺失/不足**项与改进建议。

---

## 1. 系统总览（已有能力盘点）

| 域 | 已实现 | 评估 |
|---|---|---|
| 认证 | Bearer Token（内存 Map，12h TTL）+ BCrypt | 够用，但无刷新、无多端登出 |
| 用户/权限 | `ADMIN` / `WAREHOUSE` 两硬编码角色，Service 内 `ensureAdmin()` 强判 | **不可配置**，缺角色权限表 |
| 基础资料 | 物品、分类（单层）、仓库、库位、往来单位、用户 | 缺多级分类、计量单位组、序列号/批次效期 |
| 单据 | 入库单 / 出库单（IN/OUT 同表分离），`DRAFT→APPROVED→COMPLETED` + `REJECTED/CANCELLED` | 缺退货、报损报溢、组装拆卸、红冲 |
| 调拨 | `DRAFT→APPROVED→COMPLETED`，跨仓跨库位 | **无在途**，调出即扣减、调入即增加（同步两步） |
| 盘点 | 快照 + 实盘 + 差异调整，自动 `adjust_in/out` | 够用 |
| 库存 | 加权平均成本、库存流水（append-only）、`PESSIMISTIC_WRITE` 行锁 | **无批次效期、无库龄**、无序列号 |
| 报表 | 仪表盘、库存预警（结合 7 天出库趋势）、利润、异常检测 | 缺收发存汇总、库龄、周转率、可追溯 |
| 工具 | QR（PNG/base64）、Excel 物品档导入导出、Mock OCR | 缺单据/标签打印模板 |
| 系统 | 操作日志？**无**；数据权限？**无**；字典？**无**；参数？**无** | 全部缺失 |
| 部署 | docker-compose（MySQL + server + web）；`ddl-auto: update` | 无 Flyway、无 Redis、无 OSS、无 Actuator |

---

## 2. 缺失功能矩阵（按优先级）

### P0 — 必须补齐（生产系统基石）

| # | 功能 | 缺失后果 | 标准实现 | 工作量 |
|---|---|---|---|---|
| **G1** | **操作日志 / 审计追踪** | 谁改了库存/审核了单据无法追溯；合规审计失败 | `operation_log` 表 + AOP 切面，记录登录、单据 CRUD、库存变动、状态切换 | M |
| **G2** | **退货入库 / 退货出库单** | 客户退货、向供应商退货无标准流程，业务被迫走"出库/入库"硬塞 | 复用 `StockDocument`，新增 `type=RETURN_IN/RETURN_OUT`，自动生成红字补偿流水 | M |
| **G3** | **报损 / 报溢单** | 货物损坏、过期、丢失无凭据入账，盘点外的损耗无处走 | 新增 `AdjustmentOrder`（区分报损/报溢/废弃），生成 `loss`/`gain` 流水 | M |
| **G4** | **单据反审 / 红冲机制** | 已 `COMPLETED` 单据发现错误无法撤回，必须手工改库 | 反审：`COMPLETED→APPROVED`（仅 ADMIN，生成反向流水）；红冲：复制单据负数冲销 | M |
| **G5** | **数据权限（仓库范围）** | WAREHOUSE 角色能看到所有仓库库存与单据，越权 | `UserAccount.warehouseScope`，查询/操作按 scope 过滤 | M |
| **G6** | **单据号落库序列** | 重启 `AtomicLong` 重置 → 单据号重复 | `document_sequence` 表（前缀+日期+计数），`SELECT ... FOR UPDATE` 取号 | S |

### P1 — 重要（成熟系统标配）

| # | 功能 | 缺失后果 | 标准实现 | 工作量 |
|---|---|---|---|---|
| **G7** | **库龄分析报表** | 货物存放多久无感，呆滞料无法识别 | 按入库流水 `transactionAt` 与现存批次匹配，输出 0–30/30–60/60–90/>90 天分桶 | S |
| **G8** | **收发存汇总表** | 期末对账缺汇总数据，会计核算缺基础表 | 按物品/仓库/月汇总：期初+入库-出库=期末 | S |
| **G9** | **批次效期管理（FEFO）** | 食品/医药/带保质期物料无法按批次先入先出，过期货物仍被出货 | `Item` 增加 `shelfLifeDays` + `Inventory` 增加 `productionDate/expireDate`，出库按 `expireDate ASC` 选批次 | M |
| **G10** | **角色与权限可配置** | 新增"采购员""财务""库管"角色需改代码 | `role` + `permission` + `role_permission` 三表，菜单级 + API 级权限 | L |
| **G11** | **盘点单缺明细行的完整字段** | 当前盘点只能全仓快照，无法按物品/库位/批次部分盘点 | `StocktakeRequest` 增加 `itemIds/locationCodes` 过滤 | S |
| **G12** | **API 列表统一分页** | 大部分 list 全量返回（`findAllDetailed()`），数据量大时 OOM | 全部 list 接口改 `Pageable`，沿用 `ItemController` 现有模式 | M |

### P2 — 增强（向商业系统看齐）

| # | 功能 | 缺失后果 | 工作量 |
|---|---|---|---|
| **G13** | 调拨在途（出库与入库分两步） | 实物在运输中，账面已直接划走，无法对账在途库存 | M |
| **G14** | 组装/拆卸单（BOM） | 套件打包、拆件销售无法处理 | L |
| **G15** | 往来对账/应收应付 | 销售出库不挂钩客户应收，无账期 | L |
| **G16** | 库存周转率/呆滞料报表 | 经营分析缺关键指标 | S |
| **G17** | 字典管理 + 系统参数 | 单据类型、状态枚举、参数硬编码 | S |
| **G18** | 打印模板（单据/标签） | 出入库无打印件，仓库实操不便 | M |
| **G19** | Token 刷新 + 多端登录管理 | 12h 后强制重登，无踢人 | S |
| **G20** | Flyway 数据库迁移 | `ddl-auto` 不安全，无法回滚 | M |
| **G21** | Actuator + Prometheus 指标 | 无监控 | S |

---

## 3. 本次实施的优先级（性价比最高）

受时间约束，按"投入产出比"选 **G1 + G2 + G3 + G4 + G6 + G7 + G8 + G11 + G12** 九项落地：

- **G1 操作日志**：合规底线，所有后续改动可被审计
- **G6 单据号落库**：先修，避免后续单据号又重置
- **G2 退货单 + G3 报损报溢 + G4 反审红冲**：补齐进销存核心三件套
- **G7 库龄 + G8 收发存汇总 + G11 盘点过滤**：补齐报表/盘点短板
- **G12 统一分页**：支撑大表

G5（数据权限）与 G9（批次效期）涉及面大，放第二批；G10/G14/G15/G20 价值大但工作量大，留 TODO。

---

## 4. 不在本次范围（明确说明）

- 微信小程序（README 描述但仓库无代码）
- Redis 缓存、MinIO、Nginx 旁路（README 提及但未启用）
- 委外加工、三方调拨、序列号管理、币种/税率（业务范围未达）
- OCR 真实识别（当前 Mock，保持）

---

## 5. 进度跟踪

| 阶段 | 任务 | 状态 |
|---|---|---|
| 审计 | 通读代码、对标开源 | ✅ 完成 |
| 报告 | 差距矩阵 | ✅ 本文件 |
| 后端 | G6 单据号落库 | ⏳ |
| 后端 | G1 操作日志（实体+监听器+AOP） | ⏳ |
| 后端 | G2 退货单 + G4 反审红冲 | ⏳ |
| 后端 | G3 报损报溢单 | ⏳ |
| 后端 | G7 库龄 + G8 收发存汇总报表 | ⏳ |
| 后端 | G11 盘点过滤 + G12 统一分页 | ⏳ |
| 测试 | mvn test 扩展覆盖 | ⏳ |
| 前端 | 新增页面与菜单注册 | ⏳ |
| 文档 | AGENTS/DEVELOPMENT/CLAUDE 同步 | ⏳ |
