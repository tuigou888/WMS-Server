# 仓库库存管理系统生产可交付性评估报告

- **评估日期**：2026-07-19
- **评估对象**：当前仓库中的 `wms-server`、`wms-web`、Docker Compose 与配套文档
- **目标部署端口**：后端 `8088`
- **评估方法**：源代码静态审查、现有测试产物复核、后端单元测试、前端生产构建、Compose 配置校验

## 1. 执行结论

**当前系统可以用于功能演示，也具备小范围、受控试点的基础，但不能按现状认定为可直接上线并正式交付的生产级仓库库存管理系统。**

综合成熟度建议评为 **45/100（MVP/试点级）**。

| 使用场景 | 结论 | 说明 |
|---|---|---|
| 产品演示、方案验证 | 可以 | 核心页面和主要库存流程已经可运行 |
| 测试环境/UAT | 可以 | 需明确使用测试数据并限制访问 |
| 小范围内部试点 | 有条件可以 | 必须先完成本文 P0 项，并准备人工兜底与每日核账 |
| 正式生产上线 | 暂不建议 | 数据一致性、安全、审计、迁移、备份、监控和自动化测试尚未达到上线门槛 |
| 面向客户商业交付 | 不建议 | 实现与设计文档存在明显差异，交付、运维和验收材料不完整 |

核心原因不是“系统无法运行”，而是目前仍缺少生产系统必须具备的**可证明正确、可追溯、可恢复、可运维、可持续升级**能力。

## 2. 已具备的能力

### 2.1 核心业务基础

当前代码已经覆盖以下主要流程：

- 物品档案、分类查询、往来单位、仓库和库位查询。
- 扫码/快捷入库和出库。
- 入库单、出库单的草稿、审核、执行、取消状态流程。
- 跨仓调拨与盘点调整。
- 按物品、仓库、库位维护实时库存。
- 移动加权平均成本、出库成本、销售金额和利润计算。
- 库存流水、库存预警、利润与看板报表。
- 二维码生成、物品 Excel 导入导出。
- 管理员与仓库操作员两类账号。

### 2.2 正确性方面的积极实现

- 库存变动服务使用 `@Transactional`，单次业务失败时能够整体回滚。
- 已有库存记录查询使用 `PESSIMISTIC_WRITE`，能降低同一库存记录并发扣减造成超卖的概率。
- 库存表对物品、仓库、库位组合设置了唯一约束。
- 数量、成本、金额使用 `BigDecimal`，数据库字段也定义了精度。
- 密码使用 BCrypt 保存，而非明文。
- 关键业务对象的单号字段设置了数据库唯一约束。
- 调拨到空库存时平均成本精度问题已经修复并有单元测试。

### 2.3 当前构建与基础部署状态

2026-07-19 本地复核结果：

- `mvn test`：通过，**2 个测试，0 失败**。
- `npm run build`：通过；仍有 Ant Design 分包约 1.36 MB 的体积警告。
- `docker compose config`：通过。
- 后端端口已统一为 **8088**：应用配置、Dockerfile、Compose、Vite 代理、Nginx 代理一致。
- Nginx 已配置 `/api/v1/` 反向代理和 SPA fallback。

以上说明项目具备“可以构建”和“具备容器化启动骨架”的基础，但不等于已经满足生产交付标准。

## 3. 生产就绪度评分

| 维度 | 评分 | 状态 | 评价 |
|---|---:|---|---|
| 核心业务覆盖 | 70/100 | 黄 | 基础收发存、调拨、盘点已覆盖，高级 WMS 能力不足 |
| 数据一致性与并发 | 45/100 | 红 | 库存行有锁，但单据执行、单号和首次建库存仍有并发风险 |
| 安全与权限 | 35/100 | 红 | 有认证和 BCrypt，但开发配置、默认账号、令牌与权限模型不适合生产 |
| 审计与可追溯 | 30/100 | 红 | 有库存流水和时间戳，但缺操作人、变更前后值、登录及配置审计 |
| 部署与运维 | 45/100 | 红 | Compose 可解析，但无生产 Profile、应用健康检查、TLS、资源限制和回滚方案 |
| 备份与灾难恢复 | 20/100 | 红 | 仅有 MySQL volume，未提供备份、恢复、保留和演练机制 |
| 自动化测试 | 25/100 | 红 | 后端仅 2 个计算单测，前端无测试脚本，无集成/E2E/并发测试 |
| 可维护性 | 40/100 | 红 | 大量单行压缩代码，缺少 API 文档、质量门禁和统一工程规范 |
| 文档与交付一致性 | 40/100 | 红 | README 描述了小程序、Redis、网关、JWT、SSL 等当前并不存在的能力 |

## 4. 上线阻断风险

### P0-01：生产环境默认仍按开发模式启动

证据：

- `application.yml` 默认连接内存 H2，使用 `ddl-auto: update`，并始终启用 H2 Console。
- `SecurityConfig` 将“没有激活任何 Profile”视为开发环境，并匿名放行 `/h2-console/**`。
- Compose 未设置 `SPRING_PROFILES_ACTIVE=prod`，因此容器部署仍满足上述“默认开发模式”判断。
- `DemoDataConfig` 无 Profile/开关保护：空库会自动创建 `admin/admin123`、`operator/operator123` 和演示库存。

影响：

- 环境变量遗漏时，服务可能以临时内存数据库启动，重启后数据丢失。
- 正式数据库为空时会写入已知默认账号和演示库存。
- H2 Console 在默认部署模式下被匿名放行，扩大攻击面。

上线要求：建立严格的 `dev/test/prod` 配置；生产环境必须显式指定 Profile，缺少数据库或密钥配置时启动失败；生产禁用 H2、H2 Console 和演示数据；首个管理员通过安全初始化流程创建并强制改密。

### P0-02：单号生成在重启和多实例场景下不安全

`DocumentNumberService` 和 `InventoryService` 均使用进程内 `AtomicLong` 生成当日单号。序列会在服务重启时归零，多实例之间也不共享。

影响：

- 同一天重启后容易生成已存在单号并触发唯一约束错误。
- 多副本部署会生成重复单号。
- 快捷入出库流水单号没有数据库唯一约束时，还可能出现不同业务共用同一展示单号。

上线要求：使用数据库序列表、Redis 原子序列或数据库生成策略；按业务类型和日期持久化；增加并发测试、重启测试和冲突重试。

### P0-03：单据执行缺少幂等和单据级并发控制

执行入出库单、调拨单、盘点单时，代码先读取状态，再逐行修改库存，最后把状态改为 `COMPLETED`。读取单据时没有悲观锁，也没有 `@Version` 乐观锁、执行令牌或库存流水唯一幂等键。

影响：同一张已审核单据被两个请求并发执行时，两个事务都可能读到 `APPROVED`，进而重复入库、重复出库或重复调拨。库存行锁只能串行化库存更新，不能证明“同一单据只执行一次”。

上线要求：

- 对待执行单据行加锁，或用带版本条件的原子状态更新抢占执行权。
- 库存流水增加业务单号、业务行号、事务类型的唯一幂等约束。
- API 支持请求幂等键。
- 增加重复点击、超时重试、并发执行和服务中断恢复测试。

### P0-04：认证会话与账号状态不适合生产

`TokenService` 将随机令牌和会话保存在单机内存 Map 中，令牌有效期 12 小时。

影响：

- 服务重启后所有用户掉线。
- 无法安全进行多实例横向扩容。
- 用户被停用或角色被修改后，已签发令牌中的角色快照仍可继续使用，最长可持续 12 小时。
- 缺少刷新令牌、会话查询、全端退出、密钥轮换和风险会话处置能力。
- 前端把令牌放在 `localStorage`，发生 XSS 时容易被读取。

上线要求：采用经过评审的 JWT/短期访问令牌方案，或 Redis/数据库持久会话；账号停用、改密、角色变化必须立即或在很短时间内失效；补充登录限流、失败锁定、密码策略和安全审计。

### P0-05：权限控制无法满足职责分离

除用户管理、仓库维护和审核动作外，大部分 API 只要求“已登录”。普通仓库账号可以调用物品增删改、往来单位增删改、Excel 导入以及直接入出库等接口。快捷入出库可以绕过单据审核流程直接改变库存。

同时，业务对象只记录 `reviewer`，不记录创建人、执行人；也没有禁止同一用户创建并审核自己的单据。

上线要求：形成权限矩阵，至少区分档案维护、入库、出库、调拨、盘点、审核、报表、成本/利润查看、用户管理；敏感接口在服务端强制鉴权；根据管理要求实现制单、审核、执行职责分离。

### P0-06：数据库升级和恢复能力缺失

当前使用 Hibernate `ddl-auto: update` 自动改变表结构，仓库中未发现 Flyway/Liquibase 迁移脚本。Compose 的 MySQL volume 只是数据持久化，不是备份。

影响：

- 版本升级不可审计、不可稳定复现，复杂字段和约束变更可能失败或产生不一致。
- 无法可靠执行灰度升级和回滚。
- 容器卷损坏、误删、主机故障或勒索攻击时，没有可验证恢复路径。

上线要求：引入版本化数据库迁移；生产禁用自动更新；建立全量/增量备份、异地保留、恢复演练、RPO/RTO 和发布回滚方案。

### P0-07：自动化测试不足以证明库存账正确

当前仓库后端仅有 2 个成本计算单元测试，前端没有 `test` 脚本。此前 API/UI 自动化产物证明了基础流程可操作，但对应 API 脚本不是仓库内可重复执行的 CI 测试套件。

尚未覆盖：

- MySQL 真实集成测试。
- 单据状态机和权限矩阵。
- 同单重复执行、并发出库、首次建库存竞争。
- 调拨死锁与失败重试。
- 盘点期间发生收发货。
- Excel 异常文件、超大文件和部分失败。
- 备份恢复、升级迁移、故障重启。
- 前端组件、关键流程 E2E、浏览器兼容和移动端操作。

正式上线前必须把关键库存不变量转化为自动化测试和 CI 发布门禁。

## 5. 重要业务与数据风险（P1）

### P1-01：盘点缺少冻结、截止时间和期间变动处理

创建盘点单时会复制当时账面库存；完成时却把实盘数与“执行当下的当前库存”比较并直接调整。盘点创建、实盘和执行之间仍允许正常入出库。

若盘点后又发生收发货，最终调整可能覆盖合法变动。生产方案应采用仓库/库位冻结、盘点截止流水号，或“实盘数 + 截止后变动量”的动态账面算法。

### P1-02：批次字段没有形成批次库存

入库请求和单据行中存在 `batchNo`，但库存表没有批次维度，库存流水也不保存批次。当前批次信息不会参与库存隔离、出库选择、追溯或报表。

若交付范围包含批次、有效期、先进先出、召回追踪，该功能目前不能算已实现。

### P1-03：审计信息不完整

`AuditableEntity` 只有创建和更新时间。库存流水没有操作用户；档案变更没有变更前后值；缺少登录失败、权限拒绝、导入、审核、执行、取消、用户管理等审计事件。

生产系统需要不可随意修改的业务审计日志，并能够按用户、时间、单号、物品和仓库查询。

### P1-04：停用状态未在所有库存业务中强制执行

往来单位已检查 `enabled`，但 `InventoryService` 查询物品和仓库时仅按 ID/编码查找，没有统一验证物品、仓库、库位是否启用。因此停用对象仍可能参与快捷入出库、单据、调拨或盘点。

### P1-05：库存首次创建仍存在竞争窗口

悲观锁只能锁住已经存在的库存行。两个事务首次同时向相同物品/仓库/库位入库时，都可能查不到记录并尝试插入；数据库唯一约束会保护最终唯一性，但其中一个正常业务请求可能以异常失败，当前没有捕获和重试策略。

### P1-06：成本精度和盘盈价值策略需业务确认

库存总金额按 2 位、平均成本和数量按 4 位保存。多次小数量变动可能累积舍入尾差。盘盈使用当前平均成本计价；库存为零后再出现盘盈时，平均成本可能为零。需要财务/业务明确金额精度、舍入时点、零库存盘盈估值和月末对账规则。

### P1-07：缺少已完成单据的更正/冲销机制

已完成单据不能取消，也没有红字、反向单、差错更正和原因审批流程。生产过程中一旦发生误操作，只能通过新的业务操作间接修正，账务关系和责任链不清晰。

### P1-08：Excel 导入缺少安全和原子性控制

导入接口未限制文件大小、扩展名、工作表行数和处理时长；非法数字会被静默转换成 0；方法没有事务包裹，处理到中途失败时可能保留部分写入。还需考虑压缩炸弹、公式、恶意复杂文件导致的内存和 CPU 风险。

### P1-09：查询和报表不具备大数据量保障

部分库存、流水、单据和报表查询直接读取全部记录，并在 Java 内存中聚合。数据量增长后可能导致接口变慢、内存压力和数据库全表扫描。需要分页、日期范围、聚合 SQL、必要索引和性能基线。

## 6. 运维与交付问题（P1/P2）

- 自定义 `/health` 只返回固定 `UP`，不检查数据库、磁盘或关键依赖，也没有 readiness/liveness 区分。
- 未引入 Actuator、指标、链路/关联 ID、结构化日志、集中日志、告警和运行看板。
- Compose 没有后端/Web 健康检查、重启策略、CPU/内存限制、只读文件系统和日志轮转。
- MySQL 使用 root 账号，默认密码为公开的 `wms_password`，并把 3306 发布到宿主机。
- 未提供 TLS/HTTPS、证书管理、安全响应头和入口限流。
- 未提供 Secret 管理方式，生产密码仍通过普通环境变量传递。
- 未实际完成一次“全新机器拉取镜像并 `docker compose up --build`”的完整运行验收；本次只验证了 Compose 配置可解析。
- 前端生产构建通过，但 Ant Design chunk 约 1.36 MB，首屏和弱网体验需要优化。
- 多个 Java/JS 文件被压缩为超长单行，明显降低代码审查、调试、静态分析和后续交接效率。
- 后端目录存在与 Maven 服务无关的 `package.json`/Recharts 依赖，属于工程杂项。
- 未发现 CI/CD、代码格式化、Lint、覆盖率阈值、依赖安全扫描、镜像扫描和 SBOM。
- 未提供 OpenAPI/Swagger 契约，前后端大量使用动态 `Map<String,Object>`，接口结构难以进行编译期和契约校验。

## 7. 设计文档与实际实现差异

README 声明或设计了以下能力，但当前仓库未实现或未按描述实现：

- 微信小程序端。
- Redis 缓存和基于 Redis 的单号生成。
- API Gateway。
- JWT 鉴权。
- MinIO/OSS。
- Nginx SSL/HTTPS。
- 文档中描述的完整批次/有效期模型。

此外，README 部分健康检查路径仍写作 `/api/health`，实际 context path 下为 `/api/v1/health`。

正式交付前必须把文档分为“已实现”“规划中”“不在本期范围”，避免客户依据设计稿验收未交付能力。

## 8. 建议整改路线

### 第一阶段：消除生产阻断项（建议先完成，再进入试点）

1. 建立 `prod` Profile，生产禁用 H2、Console、演示数据和 `ddl-auto:update`。
2. 移除默认账号密码，建立管理员安全初始化和强制改密。
3. 用数据库/Redis实现持久、并发安全的单号生成。
4. 实现单据执行原子抢占、库存流水幂等约束和重复请求保护。
5. 重构认证会话；实现账号/角色变更后的令牌失效。
6. 建立服务端权限矩阵和职责分离。
7. 引入 Flyway/Liquibase，生成基线和升级脚本。
8. 建立自动备份并至少完成一次从备份恢复到新环境的演练。

### 第二阶段：建立可运营能力

1. 补齐操作人、创建人、执行人和完整审计日志。
2. 引入 Actuator、数据库健康检查、指标、结构化日志和告警。
3. 加固 Docker/Nginx/MySQL：非 root 数据库账号、内网数据库、Secret、TLS、健康检查、重启策略、资源限制。
4. 把 API 集成测试、前端 E2E、MySQL/Testcontainers 测试纳入 CI。
5. 完成并发、负载、故障恢复和安全测试。
6. 为查询和报表增加分页、过滤、索引和性能基线。

### 第三阶段：补齐业务交付范围

按实际客户需求选择实现：

- 批次、有效期、序列号及 FIFO/FEFO。
- 采购订单、销售订单与收发货关联。
- 退货、冲销、差错更正。
- 库存预占、可用量、在途量。
- 多单位换算、包装规格。
- 库内移位、上架、拣货、波次。
- 货主/项目维度、条码规则、打印模板。
- 盘点冻结、动态盘点和复盘。
- 微信小程序或移动 PDA 端。

## 9. 建议上线验收门槛

只有至少满足以下条件，才建议签署“生产上线可交付”结论：

- 生产启动不包含默认账号、演示库存、H2 和 H2 Console。
- 所有 Schema 变更由版本化迁移脚本管理，并通过全新安装和跨版本升级测试。
- 同一单据并发执行 100 次，库存只发生一次变化；同一幂等请求重试结果一致。
- 并发出库不会产生负库存；首次并发入库不会产生不可解释的 500。
- 账号停用、改密或角色变更后，现有会话按安全策略及时失效。
- 每笔库存变动可追溯到业务单据、业务行和操作用户。
- 盘点期间变动规则明确并通过自动化测试。
- MySQL 备份可在独立环境成功恢复，RPO/RTO 有记录。
- 应用、数据库、磁盘、错误率、延迟和关键业务失败均有监控告警。
- CI 自动执行后端单元/集成测试、前端测试、E2E、迁移校验和安全扫描。
- 完成正式 MySQL 环境的容量/负载测试，并形成可接受的响应时间指标。
- 完成 HTTPS、Secret、最小权限和安全基线检查。
- README、API 文档、部署手册、运维手册、备份恢复手册和验收范围与实际代码一致。

## 10. 最终判断

当前仓库已经超过纯原型阶段，能够展示一个仓库进销存系统的主要业务流程；近期修复也解决了页面崩溃、调拨成本、认证状态码、生产反向代理和 8088 端口一致性等明确问题。

但从生产交付角度看，**单据幂等、单号持久化、生产配置隔离、会话与权限、数据库迁移、审计、备份恢复、监控和测试**仍存在系统性缺口。这些缺口一旦在真实仓库发生，会直接导致库存账重复、无法追责、升级失败、数据丢失或安全事件。

因此建议当前状态定级为：

> **“功能完整度较高的 MVP，可进入整改后的内部试点；暂不具备无条件正式上线和商业交付资格。”**
